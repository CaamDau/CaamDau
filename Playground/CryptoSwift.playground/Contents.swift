import UIKit
import Foundation


/*
import CryptoSwift

let str = "123456"
let key = "1111111111111111"

do{
    User.shared.account = "13726941273"
    User.shared.account
}
 
do{
    let aes = try AES(key: key.bytes, blockMode: ECB())
    
    let encryption = try aes.encrypt(str.bytes)
    let encryptionStr = String(bytes: encryption, encoding: String.Encoding.utf8)

    let encryptedBase64 = encryption.toBase64()

    let decryption = try aes.decrypt(encryption)
    let decrypted2 = try encryptedBase64?.decryptBase64ToString(cipher: aes)
}
*/
